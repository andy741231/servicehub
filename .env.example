# Copy this file to .env and fill in values for local development
# Connects to: free-test-servicehub (Azure SQL)
DATABASE_URL="sqlserver://houstonservice-test.database.windows.net:1433;database=free-test-servicehub;user=<db_user>;password=<db_password>;encrypt=true;trustServerCertificate=false;connectionTimeout=30"

JWT_SECRET=change_me_in_production
JWT_REFRESH_SECRET=change_me_too
PORT=4000
CLIENT_URL=http://localhost:3000

# Email (App 3)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Azure Storage (future)
AZURE_STORAGE_CONNECTION_STRING=
